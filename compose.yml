services:
  sheets-json-api:
    build:
      context: .
    image: sheets-json-api:local
    expose:
      - "3000"
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
    restart: unless-stopped
