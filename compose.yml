services:
  sheets-json-api:
    build:
      context: .
    image: sheets-json-api:local
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
    expose:
      - "3000"
    restart: unless-stopped
